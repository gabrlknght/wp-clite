# wp-clite
WP-CLite ( aka WP-CLI "Lite" Edition ) | Manages WordPress plugin and theme updates without requiring WP-CLI or direct database access. Uses the WordPress.org REST API for version and compatibility data, and downloads files directly from WordPress.org.
